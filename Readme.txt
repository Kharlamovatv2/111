Сначала создаем строковый массив.
Пишем метод. 
В цикле перебираются элементы массива, в теле цикла условие выборки: если есть элемент, удовлетворяющий условиям (<=3), 
тогда инициализируется новый массив такого же типа и в ячейку нового массива записывается 
значение ячейки первичного массива.